;;; scala-am.el --- php settings
;;
;;; Commentary:
;;
;; Settings related to php in here.
;;
;;; Code:

(use-package scala-mode
    :ensure t
    :defer t)

(use-package ensime
    :ensure t
    :defer t)


(provide 'scala-am)
